version: 2
updates:
  # release.yml pins its actions to commit SHAs because that job holds an npm publish
  # credential; this keeps those pins current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
